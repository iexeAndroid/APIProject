# 🚀 Как обратиться к сервису на Render.com

## 📍 Найти URL вашего сервиса

1. Войдите в свой аккаунт Render.com: https://dashboard.render.com
2. Найдите ваш сервис в списке
3. Скопируйте **URL** сервиса (обычно выглядит как: `https://your-service-name.onrender.com`)

## 🔗 Доступные endpoints

После того как вы найдете URL вашего сервиса (например, `https://api-project-xxxx.onrender.com`), вы можете обратиться к следующим endpoints:

### 1. Swagger UI (документация API)
```
https://your-service-url.onrender.com/swagger
```
или просто:
```
https://your-service-url.onrender.com
```
(если Swagger настроен как корневой путь)

### 2. API Endpoints для работы с задачами

#### Получить все задачи
```http
GET https://your-service-url.onrender.com/api/Tasks
```

#### Получить задачу по ID
```http
GET https://your-service-url.onrender.com/api/Tasks/1
```

#### Создать новую задачу
```http
POST https://your-service-url.onrender.com/api/Tasks
Content-Type: application/json

{
  "title": "Моя первая задача",
  "description": "Описание задачи",
  "isCompleted": false
}
```

#### Обновить задачу
```http
PUT https://your-service-url.onrender.com/api/Tasks/1
Content-Type: application/json

{
  "title": "Обновленный заголовок",
  "description": "Обновленное описание",
  "isCompleted": true
}
```

#### Удалить задачу
```http
DELETE https://your-service-url.onrender.com/api/Tasks/1
```

### 3. Тестовый endpoint (WeatherForecast)
```http
GET https://your-service-url.onrender.com/api/WeatherForecast
```

## 🧪 Примеры использования

### Через браузер
Просто откройте в браузере:
- `https://your-service-url.onrender.com/swagger` - для интерактивной документации
- `https://your-service-url.onrender.com/api/Tasks` - для получения всех задач

### Через curl (PowerShell)
```powershell
# Получить все задачи
curl https://your-service-url.onrender.com/api/Tasks

# Создать задачу
curl -X POST "https://your-service-url.onrender.com/api/Tasks" `
  -H "Content-Type: application/json" `
  -d '{\"title\":\"Новая задача\",\"description\":\"Описание\",\"isCompleted\":false}'

# Получить задачу по ID
curl https://your-service-url.onrender.com/api/Tasks/1

# Обновить задачу
curl -X PUT "https://your-service-url.onrender.com/api/Tasks/1" `
  -H "Content-Type: application/json" `
  -d '{\"title\":\"Обновленная задача\",\"isCompleted\":true}'

# Удалить задачу
curl -X DELETE https://your-service-url.onrender.com/api/Tasks/1
```

### Через Postman
1. Откройте Postman
2. Создайте новый запрос
3. Выберите метод (GET, POST, PUT, DELETE)
4. Введите URL: `https://your-service-url.onrender.com/api/Tasks`
5. Для POST/PUT добавьте Body → raw → JSON с данными

## ⚙️ Настройки на Render.com

Убедитесь, что в настройках сервиса на Render.com установлены:

### Environment Variables:
- `ASPNETCORE_ENVIRONMENT` = `Production` (или `Development` для Swagger)
- `ASPNETCORE_URLS` = `http://+:8080` (уже установлено в Dockerfile)
- `DATABASE_URL` = ваша строка подключения к PostgreSQL (если хотите использовать переменную окружения вместо appsettings.json)

### Health Check (опционально):
- Health Check Path: `/api/Tasks` или `/swagger`

## 🔍 Проверка работы сервиса

1. **Проверьте логи на Render.com:**
   - Откройте ваш сервис в Dashboard
   - Перейдите на вкладку "Logs"
   - Убедитесь, что нет ошибок

2. **Проверьте статус:**
   - Сервис должен быть в статусе "Live" (зеленый индикатор)

3. **Проверьте доступность:**
   - Откройте URL сервиса в браузере
   - Должна открыться Swagger UI или вернуться ответ от API

## 🐛 Решение проблем

### Сервис не отвечает
- Проверьте логи на Render.com
- Убедитесь, что сервис в статусе "Live"
- Проверьте, что порт 8080 открыт (должно быть автоматически)

### Ошибка подключения к БД
- Проверьте переменную окружения `DATABASE_URL` на Render.com
- Убедитесь, что строка подключения правильная
- Проверьте, что PostgreSQL сервис запущен и доступен

### Swagger не открывается
- Убедитесь, что `ASPNETCORE_ENVIRONMENT` не установлен в `Production` (или код обновлен для работы Swagger в Production)
- Попробуйте открыть `/swagger` напрямую

## 📝 Примечания

- Первый запрос к сервису на Render.com может занять несколько секунд (холодный старт)
- Бесплатные сервисы на Render.com могут "засыпать" после периода неактивности
- Для production рекомендуется использовать платный план

