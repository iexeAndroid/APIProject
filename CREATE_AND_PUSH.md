# 🚀 Быстрая инструкция: Создание репозитория и отправка кода

## Вариант 1: Через веб-интерфейс GitHub (РЕКОМЕНДУЕТСЯ - самый простой)

### Шаг 1: Создайте репозиторий на GitHub
1. Откройте в браузере: **https://github.com/new**
2. Войдите в свой аккаунт GitHub (iexeAndroid)
3. Заполните форму:
   - **Repository name**: `APIProject`
   - **Description** (опционально): "C# API Project with PostgreSQL"
   - Выберите **Public** или **Private**
   - ⚠️ **ВАЖНО**: НЕ ставьте галочки на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - (Мы уже создали эти файлы!)
4. Нажмите кнопку **"Create repository"**

### Шаг 2: Отправьте код
После создания репозитория выполните в PowerShell:

```powershell
git push -u origin main
```

### Шаг 3: Авторизация
При запросе введите:
- **Username**: `iexeAndroid`
- **Password**: Personal Access Token (см. ниже как создать)

---

## Вариант 2: Через GitHub CLI (автоматически)

### Шаг 1: Авторизуйтесь в GitHub CLI
Выполните в PowerShell:
```powershell
gh auth login
```
Следуйте инструкциям на экране (выберите GitHub.com, браузер, и авторизуйтесь)

### Шаг 2: Создайте и отправьте репозиторий
```powershell
gh repo create APIProject --public --source=. --remote=origin --push
```

---

## 🔑 Создание Personal Access Token (если используете Вариант 1)

1. Перейдите: **https://github.com/settings/tokens**
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: `APIProject`
   - **Expiration**: выберите срок действия (например, 90 дней)
   - **Select scopes**: отметьте ✅ **repo** (полный доступ к репозиториям)
4. Нажмите **"Generate token"** внизу страницы
5. ⚠️ **ВАЖНО**: Скопируйте токен СРАЗУ (он показывается только один раз!)
6. Используйте этот токен как пароль при `git push`

---

## ✅ Проверка после отправки

Откройте в браузере: **https://github.com/iexeAndroid/APIProject**

Вы должны увидеть все файлы проекта!

---

## 🆘 Если что-то пошло не так

### Ошибка "Repository not found"
- Убедитесь, что репозиторий создан на GitHub
- Проверьте правильность имени: `APIProject` (с большой буквы)

### Ошибка авторизации
- Используйте Personal Access Token, а не пароль от аккаунта
- Убедитесь, что токен имеет scope `repo`

### Проверка текущего статуса
```powershell
git status
git remote -v
git log --oneline -3
```

